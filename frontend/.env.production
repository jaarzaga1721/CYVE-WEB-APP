# Production Environment Variables
# This file is committed to git - safe for public values only.
# Store secrets (DB passwords, API keys) server-side only, NEVER here.

# Replace with your production backend URL once you have a host
# Example: https://api.cyve.ph or https://cyve.ph/backend/api
NEXT_PUBLIC_API_URL=https://YOUR_PRODUCTION_BACKEND_URL/backend/api
